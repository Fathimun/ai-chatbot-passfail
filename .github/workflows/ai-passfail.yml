name: AI Chatbot PASS FAIL Check

on:
  push:
  workflow_dispatch:

jobs:
  ai-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run AI chatbot consistency tests
        run: python main.py
